<Window x:Class="NetTrafficMonitor.Views.HudWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NetTraffic HUD"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" Topmost="True"
        Width="260" Height="90"
        WindowStartupLocation="Manual">

    <Border CornerRadius="8" Background="#AA1E1E1E" BorderBrush="#555" BorderThickness="1"
            Padding="12">
        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=ClickThrough}" Value="True">
                        <Setter Property="IsHitTestVisible" Value="False"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="{Binding Title}"
                       FontSize="11" Foreground="#AAA" Margin="0,0,0,4"
                       HorizontalAlignment="Center"/>

            <!-- Download -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="↓" Foreground="#4CAF50"
                       FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="DownloadText"
                       Text="0 Mb/s" Foreground="#4CAF50"
                       FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>

            <!-- Upload -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="↑" Foreground="#FF9800"
                       FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="UploadText"
                       Text="0 Mb/s" Foreground="#FF9800"
                       FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
        </Grid>
    </Border>
</Window>
